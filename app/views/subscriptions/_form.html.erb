<%= form_with(model: subscription, local: true) do |form| %>
  <% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
      <% subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :athlete_id %>
    <%= form.text_field :athlete_id, id: :subscription_athlete_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :subscription_category_id %>
  </div>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.text_field :event_id, id: :subscription_event_id %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :subscription_number %>
  </div>

  <div class="field">
    <%= form.label :tempo %>
    <%= form.time_select :tempo, id: :subscription_tempo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
